@model IEnumerable<Service>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}
 <div class="row">
            <div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                  <h4 class="card-title">Service Table</h4>
                  <a class="btn btn-success" asp-controller="Service" asp-action="Create" >Create</a> 
                  <div class="table-responsive">
                    <table class="table">
                      <thead>
                        <tr>
                          <th>Count</th>
                          <th>Image</th>
                          <th>Title</th>
                          <th>Setting</th>
                        </tr>
                      </thead>
                      <tbody>
                       @foreach (var item in Model)
                            {
                                count++;

                           <tr>
                          <td>@count</td>
                          <td>
                              <img src="~/img/@item.Image"/>
                            
                          <td>
                              <td>@item.Title</td>
                              <a class="btn btn-danger" asp-controller="Service" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                              <a class="btn btn-primary" asp-controller="Service" asp-action="Update" asp-route-id="@item.Id">Update</a>
                              <a class="btn btn-info" asp-controller="Service" asp-action="Detail" asp-route-id="@item.Id">Detail</a> 
                          
                        </tr>
                          
                      }
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          
          </div>


